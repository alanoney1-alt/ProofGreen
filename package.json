{
  "name": "proofgreen",
  "version": "1.0.0",
  "description": "ProofGreen ESG Platform",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
